using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CapaDatos
{
    class daoUsuario
    {
        public static entUsuario Login(String usuario, string password) {

            entUsiario obj = null;
            Sqlcommand cmd = null;
            SqlDataReader dr = null;
            try
            {
                Conexion cn = new Conexion();
                SqlConnection cnx, cn.connectar();
                cmd.Parameters
        
        }
    }
}
